{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
      
            <tr>
                <th>Etat</th>
                <td>{{ article.etat }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ article.sujet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_art_index') }}">back to list</a>

    <a href="{{ path('app_art_edit', {'id': article.id}) }}">edit</a>

    {{ include('art/_delete_form.html.twig') }}
{% endblock %}
